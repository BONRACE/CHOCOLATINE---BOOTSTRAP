// Point d'entrée conservé pour compatibilité — l'authentification agent /
// organisateur passe par NextAuth (voir /api/auth/[...nextauth]).
// Ce fichier documente le flux attendu côté client mobile/PWA :
//   POST /api/auth/callback/credentials { email, password }
export {};
